export enum BrowserStorageKeys {
  Mode = 'mode',
  Role = 'role',
  AccessToken = 'accessToken',
  Language = 'i18nextLng',
  Scroll = 'scroll',
}

export type BrowserStorageOptions = {
  session: boolean;
};
