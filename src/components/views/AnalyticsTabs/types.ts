export interface IAnalyticsTabs {
  convertedId: number;
}

export enum TabType {
  notifications = 'notifications',
  rates = 'rates',
  contacts = 'contacts',
  campaigns = 'campaigns',
  settings = 'settings',
}
